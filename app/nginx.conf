server {
    listen 80;
    add_header Access-Control-Allow-Origin *;
    location /biomarker-partnership/data/ {
        root /usr/share/nginx/html/;
        include /etc/nginx/mime.types;
        try_files $uri $uri/ /biomarker-partnership/data/index.html;
        autoindex on;
    }
}
